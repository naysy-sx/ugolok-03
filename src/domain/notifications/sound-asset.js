// Этап 47-довесок — звук уведомления как data-URI (единый файл сборки,
// vite-plugin-singlefile — тот же принцип, что остальные bundled-ассеты
// проекта). ЗАГЛУШКА: пользователь ещё не предоставил сам звук — backend.js's
// playSound() уже фейл-сейф на null (no-op), просто не проигрывает ничего,
// пока эта константа не заполнена реальным base64.
export const NOTIFICATION_SOUND_DATA_URI = null;
