import { signal } from "@preact/signals";

// ROOMS-SPEC.md §1.4 — "Быстрая связь" (Rooms) — состояние верхнеуровневой
// ветки (app.jsx's App()), НЕ внутри MainShell (иначе гостевые if внутри общей
// оболочки дают тот же класс расползающихся правок, что owner-scoping давался
// четырьмя заходами, см. ROOMS-SPEC). Вынесено в отдельный модуль (не app.jsx),
// потому что вход в "Быстрая связь" теперь строка списка внутри NavGroups
// (nav-groups.jsx) — тому нужен доступ к тем же сигналам, а app.jsx уже
// импортирует NavGroups (циклический импорт app.jsx↔nav-groups.jsx иначе).
export const roomsScreenActive = signal(false);
// Редизайн интерфейса, "область контента" (пользователь) — "Быстрая связь"
// модальным окном ПОВЕРХ MainShell вместо полноэкранной замены, со
// сворачиванием в активную сводку сайдбара (тот же принцип, что media-mini-bar
// у видео/музыки) — но ТОЛЬКО когда есть куда сворачивать (залогинен, есть
// MainShell/сайдбар); гостевой вход unlock.jsx's temp-chat — свой независимый
// Quick, этого сигнала не касается.
export const roomsMinimized = signal(false);
