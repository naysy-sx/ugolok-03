# Скопируйте в deploy/.env (файл в git не коммитится).
# Боевые пароли сюда, не в config.json репозитория.

INSTANCE_NAME=local
UGOLK_DOMAIN=localhost
RELAY_HOST=127.0.0.1
BLOSSOM_HOST=127.0.0.1
TURN_HOST=127.0.0.1

# Dev-значения. В проде — секрет окружения, не этот файл из git.
TURN_USERNAME=ugolok
TURN_PASSWORD=CHANGE_ME

WEB_PORT=8088
RELAY_PORT=7777
BLOSSOM_PORT=8080
TURN_PORT=3478
UPDATES_PORT=8787
